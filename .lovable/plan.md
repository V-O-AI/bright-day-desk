
# Система состояний агентов и поля ввода в чате сотрудников

## Что будет реализовано

Полная система визуальных состояний для двух частей чата:

1. **Профиль коллаборации** -- блок над/в сообщениях, показывающий какие AI-агенты сейчас работают
2. **Поле ввода** -- переключение между режимами AVAILABLE и PROCESSING

---

## Часть 1: Профиль коллаборации (4 состояния)

### SOLO
Один аватар агента с именем и ролью. Аватар плавно пульсирует, показывая активность.

### TANDEM
Два аватара рядом. Первый агент "делегировал" задачу второму. Оба с анимацией активности, между ними визуальная связь (пунктирная линия или стрелка).

### TEAM
До 3 аватаров отображаются. Если агентов больше 3 -- третий аватар заменяется на иконку "..." (MoreHorizontal) с числом дополнительных агентов.

### CLOSED
Анимации прекращаются. Последняя конфигурация аватаров остается статичной. Добавляется визуальный индикатор завершения (галочка или затухание).

---

## Часть 2: Поле ввода (2 состояния)

### AVAILABLE
- Текстовое поле активно для ввода
- Кнопка "Отправить" (Send) появляется только при наличии текста
- При пустом поле кнопка скрыта

### PROCESSING
- Текстовое поле доступно для набора текста, но отправка заблокирована
- Вместо кнопки Send всегда отображается кнопка "Остановить" (Square/StopCircle)
- Нажатие на "Остановить" переводит систему обратно в AVAILABLE

---

## Новые файлы

| Файл | Назначение |
|------|-----------|
| `src/components/chat/AgentAvatar.tsx` | Аватар одного агента с анимацией пульсации |
| `src/components/chat/AgentCollaborationBar.tsx` | Контейнер для SOLO/TANDEM/TEAM/CLOSED состояний |
| `src/hooks/useAgentProcess.ts` | Хук управления состоянием процесса (collaboration state + input state) |

## Изменяемые файлы

| Файл | Что меняется |
|------|-------------|
| `src/components/chat/MiniChat.tsx` | Интеграция AgentCollaborationBar, переключение поля ввода AVAILABLE/PROCESSING |
| `tailwind.config.ts` | Добавление keyframes для agent-pulse и log-scroll |

---

## Техническая реализация

### Модель данных (useAgentProcess.ts)

```text
type CollaborationState = "SOLO" | "TANDEM" | "TEAM" | "CLOSED";
type InputState = "AVAILABLE" | "PROCESSING";

interface AIAgent {
  id: string;
  name: string;       // "Менеджер Учета"
  role: string;       // "Координатор"
  emoji: string;      // "🤖"
}

interface AgentProcessState {
  collaboration: CollaborationState;
  inputState: InputState;
  agents: AIAgent[];
  logs: string[];
}
```

Хук предоставляет:
- `state` -- текущее состояние
- `startProcessing()` -- запускает SOLO, затем через таймеры переходит в TANDEM/TEAM
- `stopProcessing()` -- переводит в CLOSED, затем AVAILABLE
- Симуляция переходов (1.5с SOLO, 2с TANDEM, добавление логов каждые 1с)

### AgentAvatar.tsx

Компонент одного агента:
- Круглый аватар с emoji/иконкой
- Имя и роль под ним
- CSS-анимация `animate-pulse` при активном состоянии, статика при CLOSED
- `animate-scale-in` при появлении нового агента

### AgentCollaborationBar.tsx

Контейнер, который рендерит:
- При SOLO: 1 AgentAvatar по центру
- При TANDEM: 2 AgentAvatar с соединительной линией
- При TEAM: до 3 AgentAvatar, или 2 + иконка "..." если > 3
- При CLOSED: те же аватары без анимаций + иконка CheckCircle
- Под аватарами -- бегущие строки логов (fade-in снизу, fade-out сверху)

### Изменения в MiniChat.tsx

Поле ввода получает два режима:

**AVAILABLE:**
```text
[текстовое поле ........................] [Send только если есть текст]
```

**PROCESSING:**
```text
[текстовое поле (ввод можно, отправка нет)] [Stop всегда видна]
```

Над областью сообщений появляется AgentCollaborationBar при `inputState === "PROCESSING"`.

Кнопка Stop вызывает `stopProcessing()`, что останавливает анимации и возвращает AVAILABLE.

### Анимации (tailwind.config.ts)

Новые keyframes:
- `agent-pulse` -- мягкая пульсация обводки аватара (opacity 0.4 -> 1 -> 0.4, 2s)
- `log-fade-up` -- строка лога появляется снизу (translateY 10px, opacity 0 -> 1), живет 3s, затухает

---

## Порядок реализации

1. Добавить keyframes в `tailwind.config.ts`
2. Создать `useAgentProcess.ts` -- хук с состояниями и симуляцией
3. Создать `AgentAvatar.tsx` -- компонент аватара агента
4. Создать `AgentCollaborationBar.tsx` -- контейнер состояний с логами
5. Обновить `MiniChat.tsx` -- интегрировать бар коллаборации и двухрежимное поле ввода
6. Протестировать все переходы: отправка сообщения запускает SOLO -> TANDEM -> TEAM -> CLOSED -> AVAILABLE
