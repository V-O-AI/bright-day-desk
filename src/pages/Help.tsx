import { AppLayout } from "@/components/layout/AppLayout";

const Help = () => {
  return (
    <AppLayout>
      <div className="flex items-center justify-center h-full">
        <h1 className="text-2xl font-semibold text-muted-foreground">Help Center</h1>
      </div>
    </AppLayout>
  );
};

export default Help;
